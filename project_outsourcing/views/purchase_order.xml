<?xml version='1.0' encoding='utf-8'?>
<odoo>

    <record id="purchase_order_list_with_outsourcing" model="ir.ui.view">
        <field name="name">Purchase Order List: add outsourcing fields</field>
        <field name="model">purchase.order</field>
        <field name="inherit_id" ref="purchase.purchase_order_tree"/>
        <field name="arch" type="xml">
            <field name="partner_id" position="after">
                <field name="project_id"/>
                <field name="task_id"/>
            </field>
        </field>
    </record>

    <record id="purchase_order_form_with_outsourcing" model="ir.ui.view">
        <field name="name">Purchase Order Form: add outsourcing fields</field>
        <field name="model">purchase.order</field>
        <field name="inherit_id" ref="purchase.purchase_order_form"/>
        <field name="arch" type="xml">
            <field name="currency_id" position="after">
                <field name="is_outsourcing"/>
                <field name="project_id" invisible="not is_outsourcing" required="is_outsourcing" options="{'no_create': True}"/>
                <field name="task_id" invisible="not is_outsourcing" required="is_outsourcing" domain="[('project_id', '=', project_id)]" options="{'no_create': True}"/>
            </field>

            <!--
                The analytic account is propagated from the project for an outsourcing PO.
                Therefore, the analytic account should not be changeable manually.
            -->
            <xpath expr="//field[@name='order_line']//field[@name='account_analytic_id']" position="attributes">
                <attribute name="column_invisible">parent.is_outsourcing</attribute>
                <!--TODO: Result from converting 'attrs' attribute override without options for ['invisible', 'required', 'readonly'] to separate attributes
                     Remove redundant empty tags below for any of those attributes that are not present in the field tag in any of the parent views
                     If someone later adds one of these attributes in the parent views, they would likely be unaware it's still overridden in this view, resulting in unexpected behaviour, which should be avoided-->
                <!--TODO: Result from 'attrs' -> 'invisible' conversion without also overriding 'states' attribute
                     Check if this field tag contained a states attribute in any of the parent views, that should be combined into this 'invisible' attribute
                     (If any states attributes existed in parent views, they'll also be marked with a TODO)-->
                <attribute name="invisible"/>
                <attribute name="required"/>
                <attribute name="readonly"/>
            </xpath>
        </field>
    </record>

    <record id="purchase_order_search_with_outsourcing" model="ir.ui.view">
        <field name="name">Purchase Order Search: add outsourcing fields</field>
        <field name="model">purchase.order</field>
        <field name="inherit_id" ref="purchase.view_purchase_order_filter"/>
        <field name="arch" type="xml">
            <field name="partner_id" position="after">
                <field name="project_id"/>
                <field name="task_id"/>
            </field>
            <group position="before">
                <separator/>
                <filter string="Outsourcing" name="is_outsourcing" domain="[('is_outsourcing', '=', True)]"/>
            </group>
        </field>
    </record>

</odoo>